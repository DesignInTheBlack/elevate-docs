---
title: "Text"
description: A guide to the text properties in Elevate CSS.
---

## Text Property Modifiers

The following table describes the supported tokens and rules for the `text` property, along with the expected options and their output CSS.

| **Modifier**   | **Expected Values**                                                | **Example** |
|---------------------|-----------------------------------------------------------|------------|
| `font-size`         | `tiny`, `small`, `base`, `heading6`, `heading5`, `heading4`, `heading3`, `heading2`,  `heading2`, `heading1`, `eyebrow`,      | `text:small` |
| `color`             |   Colortoken (e.g., `purple-light`)   | `text:purple-light` |
| `font-family`       | `sans`, `serif`, `mono`                        | `text:sans` |
| `line-height`       | `hug`, `tight`, `snug`, `normal`, `relaxed`, `loose`,`spacious`,`extra-spacious`             | `text:spacious` |
| `letter-spacing`    | `extra-tight`, `tight`, `normal`, `wide`, `wider`, `widest` | `text:widest` |
| `text-align`        | `left`, `center`, `right`, `justify`                      | `text:left` |
| `max-width`         | `extra-narrow`,`narrow`, `medium`, `wide`, `extra-wide`                    | `text:wide` |
| `font-weight`       | `thin`,`extraLight`,`light`, `regular`, `medium`, `semibold`, `bold`,`extraBold`,`black`     | `text:bold` |
| `text-transform`    | `uppercase`, `lowercase`, `capitalize`            | `text:uppercase` |