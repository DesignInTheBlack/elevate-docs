---
title: Backgrounds
description: A guide to backgrounds in Elevate CSS.
---

Elevate CSS features a number of background properties. These properties include:

<br/>

## Setting Backgrounds

| Property   | Expected Values | Example     |
|------------|--------------------|-----------------------|
| `bg-img`   | PassThroughToken (e.g.,  `(url(https://example.com/image.jpg))` )      |  `bg-img:(url(https://example.com/image.jpg))`  |
| `bg-color`  | ColorToken (e.g., `purple+1`)      |  `bg-color:purple+1`  |
| `bg-size`  |  `cover`,`contain`   |  `bg-size:cover`  |
| `bg-repeat`| `no-repeat`, `repeat-x`, `repeat-y`, `repeat`, `space`, `round`, `initial`, `revert`, `unset`, `revert-layer` |  `bg-repeat:no-repeat`  |  
| `bg-position`| `left-top`, `left-center`, `left-bottom`, `center-top`, `center-center`, `center-bottom`, `right-top`, `right-center`, `right-bottom` |  `bg-position:center-center`  |
| `bg-attach`|`scroll`, `fixed`, `local` | `bg-attach:fixed` |
| `bg-clip`|`border`, `content`, `padding`, `text` | `bg-clip:content`|
| `bg-origin`| `border`, `content`, `padding` | `bg-origin:border`|
<br/>
<br/>


import { Aside } from '@astrojs/starlight/components';

<Aside title="'Pass Through' Means Literally">
When using PassThroughToken based properties or using it in your own custom syntax, remember that the actual CSS value is whatever you pass through. So, if you're using a PassThroughToken for a background image, for example, you must use the `url()` syntax just as you would in a CSS declaration. Noting, however, that you should omit the quatation marks.
</Aside>





